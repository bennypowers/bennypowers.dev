<time :datetime="datetime"
      @text="formatDate(datetime, { dateStyle, dayPeriod,
                                    hour, hourCycle,
                                    timeStyle, timeZoneName,
                                    day, month, year })"></time>

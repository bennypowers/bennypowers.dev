<time :datetime="datetime"
      @attributes
      @text="formatDate(datetime, { lang, dateStyle, dayPeriod,
                                    hour, hourCycle,
                                    timeStyle, timeZoneName,
                                    day, month, year })"></time>

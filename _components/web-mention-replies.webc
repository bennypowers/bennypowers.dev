<link rel="stylesheet" href="web-mention-replies.css">

<h2 webc:if="webmentions?.replies?.length">Comments</h2>

<web-mention webc:for="mention of (webmentions?.replies ?? [])
  .sort((a, b) => new Date(a.published) - new Date(b.published))"></web-mention>

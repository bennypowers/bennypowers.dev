<rh-blockquote>When I call the `open()` method,
               after all animation stops,
               the element should match the last known-good screenshot
               to within a 1% margin of error.</wh-blockquote>

